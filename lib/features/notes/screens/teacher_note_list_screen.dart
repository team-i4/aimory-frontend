import 'package:flutter/cupertino.dart';

class TeacherNoteListScreen extends StatelessWidget {
  const TeacherNoteListScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text("교사 알림장 화면", style: TextStyle(fontSize: 24)),
    );
  }
}